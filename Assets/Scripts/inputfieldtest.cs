using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;




public class inputfieldtest : MonoBehaviour
{
    //public Text intxt;
 

    // Start is called before the first frame update
    void Start()
    {
        //intxt = GetComponent<Text>();
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (intxt.text.Length == 1)
        {
            intxt.text = "00" + intxt.text;
        }
        if (intxt.text.Length.ToString() == "2")
        {
            intxt.text = "0" + intxt.text;
        }
        */
    }


    /*
    public void inputRefill()
    {

        if(intxt.text.Length == 1)
        {
            intxt.text = "00" + intxt;
        }
        else if(intxt.text.Length == 2)
        {
            intxt.text = "0" + intxt;
        }
    }

    */
}
